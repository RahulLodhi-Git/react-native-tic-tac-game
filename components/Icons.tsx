import React from 'react';
import Icon from 'react-native-vector-icons/FontAwesome';

const Icons = ({name}: any): React.JSX.Element => {
  return <Icon name={name} size={30} />;
};

export default Icons;
